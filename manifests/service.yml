apiVersion: v1
kind: Service
metadata:
    name: rtdmspaymentinstrumentmanager
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: rtdmspaymentinstrumentmanager